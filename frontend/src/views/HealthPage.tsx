import PlaceholderView from '../components/PlaceholderView';

export function HealthPage() {
  return (
    <PlaceholderView
      title="System Health"
      tag="Phase 2.1 · placeholder"
      description="Heartbeat, last cycle, last publication, and basic service status. Minimal and factual. Backed by a lightweight status/health endpoint — planned for Phase 2.3."
    />
  );
}

export default HealthPage;