import PlaceholderView from '../components/PlaceholderView';

export function PersonaPage() {
  return (
    <PlaceholderView
      title="Persona"
      tag="Phase 2.1 · placeholder"
      description="Identity, domain, editorial principles, voice, and interests — a reference point for judges checking consistency across posts. Populated from the agent record in a later phase."
    />
  );
}

export default PersonaPage;