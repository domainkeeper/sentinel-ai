import PlaceholderView from '../components/PlaceholderView';

export function ActivityPage() {
  return (
    <PlaceholderView
      title="Autonomous Activity"
      tag="Phase 2.1 · placeholder"
      description="The most demo-critical view: last cycle, next expected cycle, and a recent cycle log (published / rejected / idle). Depends on a small status endpoint (A6) — planned for Phase 2.3."
    />
  );
}

export default ActivityPage;