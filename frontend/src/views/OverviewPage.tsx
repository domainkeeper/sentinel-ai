import PlaceholderView from '../components/PlaceholderView';

export function OverviewPage() {
  return (
    <PlaceholderView
      title="Overview"
      tag="Phase 2.1 · placeholder"
      description="Identity, domain, mission line, current operational state, and a preview of recent publications. This is the 10-second read for a judge. Will be wired to the status and feed endpoints in Phase 2.2/2.3."
    />
  );
}

export default OverviewPage;